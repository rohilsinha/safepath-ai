import { router } from 'expo-router';
import { StyleSheet, Text, View } from 'react-native';
import { AppTitle, BackButton, Card, PrimaryButton, Screen } from '../../src/components/ui';
import { colors } from '../../src/theme';

export default function SosScreen() { return <Screen><BackButton onPress={() => router.back()} /><AppTitle eyebrow="Emergency support" title="SOS action" subtitle="This is a UI preview only. It does not contact anyone, call emergency services, or share your location." />
  <Card style={styles.info}><Text style={styles.infoTitle}>Before SOS is connected</Text><Text style={styles.infoBody}>Emergency contacts, communication permissions, delivery confirmation, and location-sharing consent will be required before this action can be enabled.</Text></Card><View style={styles.sosCircle}><Text style={styles.sosLabel}>SOS</Text><Text style={styles.preview}>PREVIEW</Text></View><PrimaryButton label="SOS unavailable in preview" tone="danger" disabled onPress={() => undefined} /><Text style={styles.help}>If you are in immediate danger, contact local emergency services or a trusted person directly.</Text></Screen>; }
const styles = StyleSheet.create({ info: { backgroundColor: colors.warningSoft, borderColor: '#F4D5BF', gap: 7 }, infoTitle: { color: '#9C461A', fontWeight: '900', fontSize: 16 }, infoBody: { color: '#7A4B32', lineHeight: 20 }, sosCircle: { width: 184, height: 184, borderRadius: 92, backgroundColor: colors.dangerSoft, borderWidth: 10, borderColor: '#F8CDD7', alignSelf: 'center', justifyContent: 'center', alignItems: 'center', marginVertical: 10 }, sosLabel: { color: colors.danger, fontWeight: '900', fontSize: 44, letterSpacing: 1 }, preview: { color: colors.danger, fontSize: 11, fontWeight: '900', letterSpacing: 2, marginTop: 2 }, help: { color: colors.muted, textAlign: 'center', fontSize: 13, lineHeight: 19, paddingHorizontal: 8 } });
